# Udev rule for Mayflash W012 GameCube Controller Adapter
# Allows user-space tools (like the updater and gc-remap) to access the raw HID endpoint without root
SUBSYSTEM=="hidraw", ATTRS{idVendor}=="0079", ATTRS{idProduct}=="1843", MODE="0666"
